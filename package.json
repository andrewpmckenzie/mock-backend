{
  "name": "mock-backend",
  "description": "An in-browser framework for simulating backend responses to `fetch()` and `XHR` requests.",
  "repository": "https://github.com/andrewpmckenzie/mock-backend",
  "license": "MIT",
  "devDependencies": {
    "lint-staged": "^8.1.0",
    "pre-commit": "^1.2.2",
    "tslint": "^5.12.0",
    "typescript": "^3.2.2"
  },
  "dependencies": {},
  "pre-commit": [
    "lint-staged"
  ],
  "scripts": {
    "lint-project": "tslint --project `pwd`",
    "lint-staged": "lint-staged"
  },
  "lint-staged": {
    "*.{ts, tsx, js, jsx}": [
      "tslint --fix",
      "git add"
    ]
  }
}
